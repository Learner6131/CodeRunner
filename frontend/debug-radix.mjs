import * as Tabs from "@radix-ui/react-tabs";
console.log("radix tabs keys:", Object.keys(Tabs));

import * as Slot from "@radix-ui/react-slot";
console.log("radix slot keys:", Object.keys(Slot));

import * as Select from "@radix-ui/react-select";
console.log("radix select keys:", Object.keys(Select));
