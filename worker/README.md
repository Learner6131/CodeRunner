# worker

To install dependencies:

```bash
bun install
```

To run:

```bash
bun run index.ts
```

This project was created using `bun init` in bun v1.3.14. [Bun](https://bun.com) is a fast all-in-one JavaScript runtime.

```my task
1. make the frontend like vs code
2. add java language
3. add compiler error
4. give only the proper errors
5. sandbxing.

```
